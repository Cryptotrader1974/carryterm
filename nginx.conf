server {
    listen 80;
    server_name _;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect HTTP to HTTPS once SSL is set up
    # Uncomment after running certbot:
    # return 301 https://$host$request_uri;

    # Proxy to app (HTTP only until SSL is configured)
    location / {
        proxy_pass http://app:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;   # keep WebSocket alive
    }
}

# HTTPS server — uncomment after certbot setup
# server {
#     listen 443 ssl;
#     server_name carryterm.io;   # replace with your domain
#
#     ssl_certificate /etc/letsencrypt/live/carryterm.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/carryterm.io/privkey.pem;
#
#     location / {
#         proxy_pass http://app:5000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 86400;
#     }
# }
